import type { Color, Pixels } from '../types';
export declare class PixelsToColors implements ReadableWritablePair<Array<Color>, Pixels> {
    statsMode: 'diff' | 'full';
    premultipliedAlpha: boolean;
    tint: Array<number>;
    protected _colors: Array<Color>;
    protected _rsControler: ReadableStreamDefaultController<Array<Color>>;
    protected _cache: Map<number, Map<number, number>>;
    protected _previousPixels?: Pixels;
    constructor(statsMode: 'diff' | 'full', premultipliedAlpha: boolean, tint: Array<number>);
    readable: ReadableStream<Color[]>;
    writable: WritableStream<Pixels>;
}
